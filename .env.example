# сервер API
PORT=8080
DATABASE_URL=

# боты (оставьте пустыми — заглушки всё равно работают локально)
TG_CLUB_BOT_TOKEN=
TG_DEAL_BOT_TOKEN=
TG_TREASURY_BOT_TOKEN=
TG_ARBITER_BOT_TOKEN=
TG_OPS_BOT_TOKEN=

# вотчер (режим без внешних сервисов)
MOCK_CHAIN=true
MOCK_TOKEN=USDT
MOCK_INTERVAL_MS=7000

# Treasury approvals (Week 2)
TREASURY_2FA_CODE=123456     # заглушка 2FA (можете поменять)
CAP_MAX_PER_TX_USD=1000      # лимит на одну выплату (1:1 к USDT/USDC)

# Payout engine picks which status
ENGINE_PICK_STATUS=approved  # движок берёт только approved (не queued)
ENGINE_TICK_MS=3000
ENGINE_STEP_MS=2000
ENGINE_BATCH=3

# Webhooks (optional; if empty, events are skipped)
WEBHOOK_URL=
WEBHOOK_SECRET=change_me
WEBHOOK_TIMEOUT_MS=3000

# Week 3: EOD engine (mock)
EOD_TICK_MS=5000

# Week 3: Ops alerts (optional webhook)
OPS_ALERT_WEBHOOK_URL=
OPS_ALERT_SECRET=change_me
OPS_STUCK_QUEUED_MIN=15
OPS_STUCK_BROADCAST_MIN=30

# Week 4: limits
CAP_PER_DEAL_USD=500
CAP_OPEN_EXPOSURE_USD=2000
LIMIT_K_S=1.0
LIMIT_K_M=2.0
LIMIT_K_L=3.0
LIMIT_K_XL=5.0